
import { isFirstDayOfWeek } from 'dummy/helpers/is-first-day-of-week';
import { module, test } from 'qunit';

module('Unit | Helper | is first day of week');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isFirstDayOfWeek([42]);
  assert.ok(result);
});

